import React, { Component } from 'react';

export default class App extends Component {
  render() {
    return (
      <div className="App">
        React Redux Sample!
        {/* write switch component for example */}
      </div>
    );
  }
}
